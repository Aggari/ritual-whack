export const metadata = { title: "Ritual Whack", description: "Threats incoming. Hit only the right defenders." };
export default function RootLayout({ children }) {
  return (<html lang="en"><body style={{margin:0,padding:0,background:"#050505"}}>{children}</body></html>);
}
